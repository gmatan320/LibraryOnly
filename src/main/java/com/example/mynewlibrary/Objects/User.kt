package com.example.mynewlibrary

class User {
    var username: String
    var isFollow: Boolean

    constructor(username: String, isFollow: Boolean) {
        this.username = username
        this.isFollow = isFollow
    }
}

